<div class="container py-5">
    <div class="row">
        <div class="col-md-12">
            <div class="row">
                <div class="col-md-6 mx-auto">
                    <!-- form card login -->
                    <div class="card card-signin my-5">
                        <div class="card-header">
                            <h3 class="mb-0"><%= Building.find(params[:building_id]).building_name %> 방 추가하기</h3>
                        </div>
                        <div class="card-body">
                            <form action="/rooms/create" method="POST" clsss="rounded-pill">
                                <div class="form-group">
                                    <label for="uname1">방 번호</label>
                                     <input type="text" name="room_number" class="form-control form-control-lg rounded-0">
                                </div>
                                <input type="hidden" value="<%= @building_id %>" name="building_id">
                    <button type="submit" class="btn btn-success btn-lg float-right rounded-pill">방 추가하기</button>