<form action="/posts/update/<%= @post.id %>" method="POST">
    <input type="text" name="title" value="<%= @post.title %>">
    <textarea name="content" class="tinymce" rows="40"><%= @post.content %> </textarea>
    <%= tinymce %>
    <br>
    <button type="submit">수정</button>
</form>